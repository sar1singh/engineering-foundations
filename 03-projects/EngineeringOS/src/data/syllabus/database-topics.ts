import type { SyllabusPracticeProblem, SyllabusTopic } from "@/types/syllabus";

const databaseReferences = [
  { id: "reference-db-sqlbolt", title: "SQLBolt interactive SQL lessons", url: "https://sqlbolt.com/", sourceType: "practice" as const, usage: "Practice platform from the roadmap for SELECT, JOIN, GROUP BY, subqueries, and SQL execution order." },
  { id: "reference-db-leetcode-sql", title: "LeetCode SQL practice", url: "https://leetcode.com/problemset/database/", sourceType: "practice" as const, usage: "Roadmap practice platform for SQL interview query drills." },
  { id: "reference-db-postgres-indexes", title: "PostgreSQL Indexes documentation", url: "https://www.postgresql.org/docs/current/indexes.html", sourceType: "docs" as const, usage: "Official PostgreSQL reference for indexes and query performance foundations." },
  { id: "reference-db-postgres-explain", title: "PostgreSQL EXPLAIN documentation", url: "https://www.postgresql.org/docs/current/using-explain.html", sourceType: "docs" as const, usage: "Official PostgreSQL reference for reading query plans." },
  { id: "reference-db-mongodb-sharding", title: "MongoDB Sharding documentation", url: "https://www.mongodb.com/docs/manual/sharding/", sourceType: "docs" as const, usage: "Official MongoDB reference for distributing large data sets and throughput." },
  { id: "reference-db-redis-cache", title: "Redis caching documentation", url: "https://redis.io/docs/latest/develop/use-cases/prefetch-cache/", sourceType: "docs" as const, usage: "Redis reference for cache patterns and cache-aside behavior." },
  { id: "reference-db-redis-eviction", title: "Redis key eviction documentation", url: "https://redis.io/docs/latest/develop/reference/eviction/", sourceType: "docs" as const, usage: "Redis reference for TTL, memory limits, and eviction behavior." }
];

function dbProblems(slug: string, title: string): SyllabusPracticeProblem[] {
  return [
    { id: `problem-db-${slug}-easy`, title: `${title} easy drill`, difficulty: "easy", tags: ["database", slug, "local-db"], prompt: `Solve a small ${title} exercise in SQLBolt, LeetCode SQL, or a local DB. Explain the result shape.`, expectedSignals: ["Runs a query or local lab", "Explains result shape"] },
    { id: `problem-db-${slug}-medium`, title: `${title} backend scenario`, difficulty: "medium", tags: ["database", slug, "backend-design"], prompt: `Apply ${title} to a backend feature. Include schema/query shape, edge cases, and trade-off.`, expectedSignals: ["Connects database choice to feature", "Names trade-off"] },
    { id: `problem-db-${slug}-hard`, title: `${title} production review`, difficulty: "hard", tags: ["database", slug, "production"], prompt: `Diagnose a production issue involving ${title}. Include measurements, root cause, and safe remediation.`, expectedSignals: ["Uses evidence", "Explains risk", "Defines verification"] }
  ];
}

function dbTopic(input: {
  order: number;
  slug: string;
  title: string;
  phase: string;
  definition: string;
  mentalModel: string;
  theory: string;
  code: string;
  interviewQuestions: string[];
  commonMistakes: string[];
  productionUseCases: string[];
}): SyllabusTopic {
  return {
    id: `syllabus-db-${input.slug}`,
    slug: input.slug,
    title: input.title,
    order: input.order,
    sourcePath: "00-control/master-roadmap/05-databases/INDEX.md",
    definition: input.definition,
    whyItMatters: `${input.title} is part of Database ${input.phase}. It supports SQL interviews and backend data-decision judgment.`,
    mentalModel: input.mentalModel,
    theory: `${input.theory}\n\nVisual model: question -> data shape -> access pattern -> correctness/performance trade-off.`,
    codeExamples: [{ id: `example-db-${input.slug}`, title: `${input.title} example`, language: input.code.trim().startsWith("db.") ? "javascript" : "sql", code: input.code, explanation: `80/20 working example for ${input.title}.`, runnable: true }],
    practiceProblems: dbProblems(input.slug, input.title),
    interviewQuestions: input.interviewQuestions,
    commonMistakes: input.commonMistakes,
    productionUseCases: input.productionUseCases,
    revisionPrompts: [`Explain ${input.title} with one query or backend example.`, `Name one edge case for ${input.title}.`, `Name one performance or correctness trade-off.`],
    reviewPrompts: [{ id: `review-db-${input.slug}-mentor`, reviewerRole: "mentor", prompt: `Review ${input.title} like a backend/data interview answer.`, rubric: ["Correct result", "Trade-off clear", "Production risk named"] }],
    references: [...databaseReferences, { id: `reference-db-${input.slug}-roadmap`, title: "EngineeringOS Databases master roadmap", url: "00-control/master-roadmap/05-databases/INDEX.md", sourceType: "roadmap", usage: "Local source of truth for Database ordering and practice platforms." }],
    progressSignals: ["read_definition", "read_theory", "studied_code_example", "ran_code_example", "solved_easy_problem", "solved_medium_problem", "solved_hard_problem", "submitted_explain_back", "completed_mock_review", "scheduled_revision"]
  };
}

export const databaseSqlCoreTopics: SyllabusTopic[] = [
  dbTopic({ order: 1, slug: "select", title: "SELECT", phase: "SQL Core", definition: "SELECT retrieves columns and expressions from one or more tables.", mentalModel: "Choose rows, choose columns, shape the result.", theory: "SELECT is the base of every SQL interview. Know projection, filtering, ordering, limiting, aliases, NULL behavior, and execution order.", code: "SELECT id, name\nFROM users\nWHERE active = true\nORDER BY created_at DESC\nLIMIT 10;", interviewQuestions: ["What is the difference between WHERE and HAVING?", "How does NULL affect filters?", "What is SQL execution order?"], commonMistakes: ["Selecting too many columns", "Forgetting NULL behavior", "Assuming output order without ORDER BY"], productionUseCases: ["API list endpoints", "Admin dashboards", "Reporting queries"] }),
  dbTopic({ order: 2, slug: "joins", title: "JOINS", phase: "SQL Core", definition: "JOIN combines rows from multiple tables using matching keys or conditions.", mentalModel: "Bring related facts together by keys; join type controls what survives.", theory: "INNER JOIN keeps matches; LEFT JOIN preserves left rows; many-to-many joins can multiply rows. Always know cardinality before aggregating.", code: "SELECT users.name, orders.total\nFROM users\nJOIN orders ON orders.user_id = users.id;", interviewQuestions: ["INNER JOIN vs LEFT JOIN?", "Why do joins duplicate rows?", "How do you debug missing joined data?"], commonMistakes: ["Wrong join type", "Joining on non-unique keys", "Aggregating after accidental row multiplication"], productionUseCases: ["User orders", "Permissions", "Analytics joins"] }),
  dbTopic({ order: 3, slug: "group-by", title: "GROUP BY", phase: "SQL Core", definition: "GROUP BY collapses rows into groups so aggregate functions can compute per-group values.", mentalModel: "Partition rows, then summarize each partition.", theory: "GROUP BY powers counts, sums, averages, and grouped reporting. HAVING filters groups after aggregation.", code: "SELECT status, COUNT(*) AS count\nFROM orders\nGROUP BY status\nHAVING COUNT(*) > 10;", interviewQuestions: ["WHERE vs HAVING?", "Why must non-aggregated columns appear in GROUP BY?", "How do NULL groups behave?"], commonMistakes: ["Filtering aggregates in WHERE", "Grouping by too many columns", "Counting nullable columns incorrectly"], productionUseCases: ["Metrics", "Billing summaries", "Operational dashboards"] }),
  dbTopic({ order: 4, slug: "subqueries", title: "Subqueries", phase: "SQL Core", definition: "A subquery is a query nested inside another query to provide a value, set, or derived table.", mentalModel: "Use one query to feed another query.", theory: "Subqueries can live in SELECT, FROM, WHERE, or HAVING. They are useful for decomposition but can be replaced with joins or CTEs when readability or performance improves.", code: "SELECT name\nFROM users\nWHERE id IN (SELECT user_id FROM orders WHERE total > 100);", interviewQuestions: ["When use subquery vs join?", "What is a correlated subquery?", "How can subqueries affect performance?"], commonMistakes: ["Writing unreadable nesting", "Ignoring correlated subquery cost", "Returning multiple rows where one value is expected"], productionUseCases: ["Eligibility filters", "Derived tables", "Reporting"] }),
  dbTopic({ order: 5, slug: "window-functions", title: "Window Functions", phase: "SQL Core", definition: "Window functions compute values across related rows while keeping each row visible.", mentalModel: "Aggregate over a window without collapsing rows.", theory: "Window functions unlock ranking, running totals, lag/lead comparisons, and top-N-per-group problems. They are high ROI for SQL interviews.", code: "SELECT user_id, total,\n  RANK() OVER (PARTITION BY user_id ORDER BY total DESC) AS order_rank\nFROM orders;", interviewQuestions: ["Window function vs GROUP BY?", "What does PARTITION BY do?", "How solve top N per group?"], commonMistakes: ["Expecting rows to collapse", "Wrong partition", "No deterministic ORDER BY"], productionUseCases: ["Leaderboards", "Cohort analytics", "Fraud/ranking queries"] })
];

export const databasePerformanceTopics: SyllabusTopic[] = [
  dbTopic({ order: 6, slug: "indexes", title: "Indexes", phase: "Performance", definition: "An index is an auxiliary data structure that speeds reads by maintaining searchable order or lookup metadata.", mentalModel: "Pay write/storage cost to reduce read search cost.", theory: "Indexes help WHERE, JOIN, ORDER BY, and uniqueness. The 80/20 rule: index high-selectivity predicates and join keys, then verify with EXPLAIN.", code: "CREATE INDEX idx_orders_user_created\nON orders (user_id, created_at DESC);", interviewQuestions: ["When does an index help?", "What is a composite index?", "Why can indexes slow writes?"], commonMistakes: ["Indexing every column", "Wrong column order", "Not checking query plan"], productionUseCases: ["Low-latency lookups", "Pagination", "Foreign-key joins"] }),
  dbTopic({ order: 7, slug: "query-tuning", title: "Query Tuning", phase: "Performance", definition: "Query tuning improves SQL performance by changing query shape, indexes, schema, or access pattern based on measurements.", mentalModel: "Make the database do less work for the same correct result.", theory: "Tune by identifying slow queries, reading plans, reducing scanned rows, removing accidental row multiplication, and avoiding unnecessary sorting or repeated work.", code: "SELECT user_id, COUNT(*)\nFROM orders\nWHERE created_at >= CURRENT_DATE - INTERVAL '30 days'\nGROUP BY user_id;", interviewQuestions: ["How do you approach a slow query?", "What measurements matter?", "When denormalize?"], commonMistakes: ["Guessing without EXPLAIN", "Ignoring data volume", "Fixing one query by hurting writes badly"], productionUseCases: ["Slow endpoints", "Analytics jobs", "Database cost control"] }),
  dbTopic({ order: 8, slug: "explain-plan", title: "Explain Plan", phase: "Performance", definition: "An explain plan shows how the database intends to execute a query, including scans, joins, sorting, and estimated cost.", mentalModel: "Read the database's execution strategy before guessing.", theory: "EXPLAIN reveals sequential scans, index scans, join algorithms, row estimates, and sort steps. It is the bridge between SQL text and actual performance.", code: "EXPLAIN ANALYZE\nSELECT * FROM orders WHERE user_id = 42;", interviewQuestions: ["What do sequential scan and index scan mean?", "Why can row estimates be wrong?", "What does EXPLAIN ANALYZE add?"], commonMistakes: ["Only reading total cost", "Ignoring row estimates", "Running ANALYZE carelessly on heavy production queries"], productionUseCases: ["Performance debugging", "Index validation", "Migration review"] })
];

export const databasePostgresTopics: SyllabusTopic[] = [
  dbTopic({ order: 9, slug: "mvcc", title: "MVCC", phase: "PostgreSQL", definition: "MVCC lets PostgreSQL provide transaction isolation by keeping multiple row versions visible to different transactions.", mentalModel: "Readers see a snapshot while writers create new versions.", theory: "MVCC reduces read/write blocking but creates vacuum and bloat considerations. It explains many isolation and visibility behaviors.", code: "BEGIN;\nSELECT * FROM accounts WHERE id = 1;\n-- snapshot visibility depends on isolation level\nCOMMIT;", interviewQuestions: ["What problem does MVCC solve?", "Why is vacuum needed?", "How does isolation affect visibility?"], commonMistakes: ["Assuming reads always block writes", "Ignoring long transactions", "Not understanding snapshots"], productionUseCases: ["High-concurrency reads", "Transaction debugging", "Vacuum/bloat operations"] }),
  dbTopic({ order: 10, slug: "locks", title: "Locks", phase: "PostgreSQL", definition: "Locks coordinate concurrent access so transactions do not corrupt shared database state.", mentalModel: "Locks protect invariants but can block progress.", theory: "PostgreSQL uses row/table/advisory locks. The 80/20 backend skill is recognizing blocking, deadlocks, and transaction scope.", code: "BEGIN;\nSELECT * FROM jobs WHERE id = 1 FOR UPDATE;\nUPDATE jobs SET status = 'processing' WHERE id = 1;\nCOMMIT;", interviewQuestions: ["Row lock vs table lock?", "What causes deadlocks?", "How reduce lock contention?"], commonMistakes: ["Keeping transactions open too long", "Locking rows in inconsistent order", "Doing network calls inside transactions"], productionUseCases: ["Payment updates", "Job claiming", "Inventory changes"] }),
  dbTopic({ order: 11, slug: "partitioning", title: "Partitioning", phase: "PostgreSQL", definition: "Partitioning splits one logical table into smaller physical pieces based on a key such as time or tenant.", mentalModel: "Keep the table interface, divide the storage and scans.", theory: "Partitioning helps very large tables when queries naturally target a subset. It adds design and maintenance complexity.", code: "CREATE TABLE events (\n  id bigint,\n  created_at date NOT NULL\n) PARTITION BY RANGE (created_at);", interviewQuestions: ["When partition a table?", "What is partition pruning?", "What key would you choose?"], commonMistakes: ["Partitioning too early", "Choosing a key not used by queries", "Forgetting maintenance"], productionUseCases: ["Event logs", "Time-series data", "Large multi-tenant tables"] }),
  dbTopic({ order: 12, slug: "replication-basics", title: "Replication Basics", phase: "PostgreSQL", definition: "Replication copies database changes to another server for read scaling, high availability, or disaster recovery.", mentalModel: "Primary accepts writes; replicas follow with possible lag.", theory: "Replication improves resilience and read capacity, but introduces lag, failover complexity, and consistency trade-offs.", code: "-- Backend routing idea\n-- writes -> primary\n-- stale-tolerant reads -> replica", interviewQuestions: ["What is replication lag?", "When read from replica?", "What happens during failover?"], commonMistakes: ["Reading fresh-after-write data from lagging replicas", "No failover plan", "Ignoring replica monitoring"], productionUseCases: ["Read scaling", "HA", "Backups", "Reporting replicas"] })
];

export const databaseMongoTopics: SyllabusTopic[] = [
  dbTopic({ order: 13, slug: "mongodb-schema-design", title: "MongoDB Schema Design", phase: "MongoDB", definition: "MongoDB schema design chooses document shape, embedding, references, and indexes based on query and update patterns.", mentalModel: "Model data around access patterns, not just entities.", theory: "Embedding is good for data read together; referencing is better for independent growth or many-to-many relationships. Indexes still matter.", code: "db.orders.find({ userId: 'u1' }, { total: 1, status: 1 });", interviewQuestions: ["Embed vs reference?", "How do query patterns shape schema?", "What index supports this query?"], commonMistakes: ["Relational modeling by habit", "Unbounded arrays", "No indexes for query patterns"], productionUseCases: ["Product catalogs", "User activity", "Document APIs"] }),
  dbTopic({ order: 14, slug: "mongodb-aggregation", title: "MongoDB Aggregation", phase: "MongoDB", definition: "MongoDB aggregation processes documents through pipeline stages to filter, group, join, reshape, and compute results.", mentalModel: "Documents flow through a pipeline of transformations.", theory: "Aggregation is MongoDB's reporting and transformation engine. Push selective $match stages early and understand memory/cardinality costs.", code: "db.orders.aggregate([\n  { $match: { status: 'paid' } },\n  { $group: { _id: '$userId', total: { $sum: '$total' } } }\n]);", interviewQuestions: ["Why put $match early?", "What does $group do?", "When use aggregation vs app code?"], commonMistakes: ["Late filtering", "Large unwound arrays", "Ignoring index use before aggregation"], productionUseCases: ["Reports", "Analytics", "Search facets"] }),
  dbTopic({ order: 15, slug: "mongodb-sharding-basics", title: "MongoDB Sharding Basics", phase: "MongoDB", definition: "MongoDB sharding distributes data across shards to support large data sets and high throughput.", mentalModel: "Shard by a key that spreads load and matches access patterns.", theory: "Sharding adds capacity but makes shard-key choice critical. Poor shard keys create hotspots, scatter-gather queries, and migration pain.", code: "sh.shardCollection('app.events', { tenantId: 1, createdAt: 1 });", interviewQuestions: ["What makes a good shard key?", "What is a hotspot?", "What is scatter-gather?"], commonMistakes: ["Bad shard key", "Sharding too early", "Ignoring query routing"], productionUseCases: ["Large multi-tenant workloads", "High-write event data", "Horizontal scaling"] })
];

export const databaseRedisTopics: SyllabusTopic[] = [
  dbTopic({ order: 16, slug: "redis-cache-patterns", title: "Cache Patterns", phase: "Redis", definition: "Cache patterns define how application data is read from, written to, invalidated, and refreshed in Redis.", mentalModel: "Cache is a fast copy with correctness rules.", theory: "Cache-aside is the 80/20 pattern: read cache, fetch source on miss, write cache with TTL. Consider stampede, stale data, and invalidation.", code: "const cached = await redis.get(key);\nif (cached) return JSON.parse(cached);\nconst value = await db.load(id);\nawait redis.set(key, JSON.stringify(value), { EX: 60 });", interviewQuestions: ["What is cache-aside?", "How handle stale data?", "What is cache stampede?"], commonMistakes: ["No TTL", "Caching wrong data", "Ignoring invalidation"], productionUseCases: ["Hot reads", "Session lookups", "Expensive computations"] }),
  dbTopic({ order: 17, slug: "redis-ttl", title: "TTL", phase: "Redis", definition: "TTL sets an expiration time so Redis keys are automatically removed after a duration.", mentalModel: "Every cached value should have a freshness contract.", theory: "TTL limits stale data and memory growth. Choose TTL from data volatility, user impact, and backend load tolerance.", code: "await redis.set('user:42', JSON.stringify(user), { EX: 300 });\nconst ttl = await redis.ttl('user:42');", interviewQuestions: ["How choose TTL?", "What happens when many keys expire together?", "How does TTL relate to eviction?"], commonMistakes: ["No expiry", "Same TTL causing stampedes", "Assuming TTL guarantees correctness"], productionUseCases: ["Sessions", "Cached profiles", "Cooldowns", "Temporary locks"] }),
  dbTopic({ order: 18, slug: "redis-rate-limiting", title: "Rate Limiting", phase: "Redis", definition: "Redis rate limiting uses shared counters, sorted sets, or token buckets to control request volume across distributed app instances.", mentalModel: "A shared fast counter enforces traffic budgets.", theory: "Redis fits rate limiting because all app instances can check the same state quickly. Use atomic operations and TTLs to avoid race conditions and leaked counters.", code: "const key = `rate:${userId}`;\nconst count = await redis.incr(key);\nif (count === 1) await redis.expire(key, 60);\nif (count > 100) throw new Error('rate limited');", interviewQuestions: ["Fixed window vs sliding window?", "Why use Redis for distributed rate limits?", "What race condition can happen?"], commonMistakes: ["Non-atomic checks", "No TTL", "Global limit too coarse"], productionUseCases: ["API abuse protection", "Login throttling", "Tenant quotas"] })
];
