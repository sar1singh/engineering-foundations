/**
 * The "Module" Pattern: Creating private state using closures.

Prototype Pollution/Lookup: Predicting output when a property is modified on Object.prototype or within a chain.

Closure in Loops: The classic for(var i...) vs for(let i...) interview challenge
 */